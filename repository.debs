# /bin/bash

### install debian packages
sudo apt-get install -y  python-setuptools ros-hydro-ros ros-hydro-vision-opencv python-rosinstall python-vcstools ros-hydro-brics-actuator liblog4cpp5-dev ros-hydro-rosjava-core ros-hydro-bfl

### install further repositories
rosinstall .. /opt/ros/hydro repository.rosinstall

### install dependencies of BRSU repositories
dependent_repositories=('mas_third_party_software')
for i in $dependent_repositories
do
    ../$i/repository.debs
done
